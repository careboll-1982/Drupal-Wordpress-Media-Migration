# Security

Please report security issues privately to the repository owner rather than in
a public issue. Migration CSV files can disclose filenames and source URLs;
remove public manifests immediately after use.

The optional invalid-certificate override is intended only for a source host
you control. It is scoped to source downloads and is disabled by default.

